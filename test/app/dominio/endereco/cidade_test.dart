import 'package:flutter_test/flutter_test.dart';
import 'package:app_motorista/app/dominio/cidade.dart';

void main() {
  
  group('Testes Cidade', () {
    test('Teste de nome de cidade vazio', () {
      //expect(() => Cidade(nome: ''), throwsException);
    });
    test('Teste de nome de cidade não vazio', () {
      //var cidade = Cidade(nome: 'Paranavaí');
      //expect(cidade.getNome(), 'Paranavaí');
    });
  });
}
